#name is the parameter that hello needs when defined
def hello(name):
    print('Hello, ' + name + '!')

#'Alice' and 'Bob' are the arguments being passed into the already defined function during its calling phase
hello('Alice')
hello('Bob')
